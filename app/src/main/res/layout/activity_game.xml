qqq<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_tapete_juego">
    
    <LinearLayout
        android:id="@+id/headerLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="80dp"
        android:orientation="horizontal"
        android:padding="16dp"
        android:background="@drawable/card_fondo_papel"
        android:elevation="4dp"
        android:gravity="center_vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Jugador 1 -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="center"
            android:paddingHorizontal="8dp">

            <TextView
                android:id="@+id/tvPlayer1Name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Jugador 1"
                android:maxLines="1"
                android:ellipsize="end"
                style="@style/TextAppearance.TapDuel.Rustica"
                android:textSize="16sp"
                android:textStyle="bold"/>

            <TextView
                android:id="@+id/tvPlayer1Score"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="0"
                android:textColor="@color/color_principal_madera"
                android:textSize="32sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-medium"
                android:layout_marginTop="4dp"/>

        </LinearLayout>

        <!-- Divisor -->
        <View
            android:layout_width="2dp"
            android:layout_height="50dp"
            android:background="@color/color_acento_dorado"
            android:alpha="0.8"
            android:layout_marginHorizontal="12dp"/>

        <!-- Jugador 2 -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="center"
            android:paddingHorizontal="8dp">

            <TextView
                android:id="@+id/tvPlayer2Name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Esperando..."
                android:maxLines="1"
                android:ellipsize="end"
                style="@style/TextAppearance.TapDuel.Rustica"
                android:textSize="16sp"
                android:textStyle="bold"/>

            <TextView
                android:id="@+id/tvPlayer2Score"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="0"
                android:textColor="@color/error_color"
                android:textSize="32sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-medium"
                android:layout_marginTop="4dp"/>

        </LinearLayout>

    </LinearLayout>

    <!-- CANVAS DEL JUEGO -->
    <com.example.proyectofina3.GameCanvasView
        android:id="@+id/gameCanvasView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/headerLayout"
        app:layout_constraintBottom_toTopOf="@id/footerLayout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- FOOTER -->
    <LinearLayout
        android:id="@+id/footerLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="@drawable/card_fondo_papel"
        android:elevation="4dp"
        app:layout_constraintBottom_toBottomOf="parent">

        <!-- Código de sala -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginBottom="8dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Código de sala: "
                android:textColor="@color/color_texto_oscuro"
                android:textSize="18sp"
                style="@style/TextAppearance.TapDuel.Rustica"/>

            <TextView
                android:id="@+id/tvRoomCode"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="0000"
                android:textColor="@color/color_acento_dorado"
                android:textSize="20sp"
                android:textStyle="bold"
                android:fontFamily="monospace"/>

            <ImageButton
                android:id="@+id/btnCopyCode"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:src="@drawable/ic_copy"
                android:background="?attr/selectableItemBackgroundBorderless"
                app:tint="@color/color_acento_dorado"
                android:contentDescription="Copiar código"
                android:layout_marginStart="8dp"/>

        </LinearLayout>

        <!-- Estado del juego -->
        <TextView
            android:id="@+id/tvGameStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="Esperando jugadores..."
            android:textColor="@color/color_principal_madera"
            android:textSize="18sp"
            android:textStyle="italic"
            android:alpha="0.8"
            style="@style/TextAppearance.TapDuel.Rustica"
            android:layout_marginBottom="12dp"/>

        <!-- Botón salir -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnLeaveRoom"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="SALIR DE LA SALA"
            style="@style/Widget.Material3.Button.OutlinedButton"
            app:cornerRadius="24dp"
            android:textSize="14sp"
            app:strokeColor="@color/error_color"
            app:strokeWidth="2dp"
            android:textColor="@color/error_color"/>

    </LinearLayout>

    <!-- LOADING OVERLAY -->
    <FrameLayout
        android:id="@+id/loadingOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#992F241F"
        android:visibility="gone"
        android:clickable="true"
        android:focusable="true">

        <ProgressBar
            android:id="@+id/progressBar"  android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:indeterminateTint="@color/color_acento_dorado"/>

        <Button
            android:id="@+id/btnStartGame"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="INICIAR JUEGO"
            android:textSize="18sp"
            android:textStyle="bold"
            android:visibility="gone" android:layout_gravity="center"
            android:backgroundTint="@color/color_acento_dorado"
            android:textColor="@android:color/white"
            android:padding="16dp"/>

    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
